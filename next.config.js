/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', "www.pexels.com", "th.bing.com", "www.aggreko.com"], 
    }
}

module.exports = nextConfig
